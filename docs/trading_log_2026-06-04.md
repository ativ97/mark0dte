# Trading Log — 2026-06-04 (Thursday) — DATA DAY (light-touch, fixed-size out-of-sample)
# Mode: LIGHT-TOUCH (NOT a hypothesis-grading session). The two structured sessions (6/2, 6/3)
# did their job; today is day 1 of the discipline from docs/method_analysis_2026-06-03.md:
# disciplined fixed-size trading to build the ~30–50-trade out-of-sample expectancy sample,
# plus a live smoke-test of yesterday's build. Catalyst: NFP tomorrow (Fri 6/5) — watch positioning.

## TODAY'S DISCIPLINE (the only rules that matter)
- **SIZE is the whole game.** Keep total book max-loss ≤ the amber line (~$3,750 / 25% of acct).
  The new Tail-Day Preview banner shows it live — size until it's amber-or-better, NEVER red.
- **Fixed size all day.** Pick one size and hold it. The edge is proven; size is the only leak.
- **Freeze the core.** No engine/threshold tuning today. This is data collection, not model-fitting.
- **Act on the P0-2 force-path if it fires** (neg-GEX trend-through, short the side it's moving toward)
  — obeying it once at fixed size is the H7 clean-proof we still lack.

## PRE-OPEN BUILD SMOKE TEST (confirm before trusting the new panels)
- [~] `pytest` / `synthetic_replay` — confirm in mark env (uvicorn boots clean → live telemetry flowing ✓)
- [x] telemetry **serializes** `tail_day_preview` + `gex_velocity` cleanly (live JSON 13:31, no Pydantic crash) ✓
- [~] Tail-Day Preview — data live & correct ("No open positions"); **visual render pending first position**
- [x] **ΔGEX velocity populates** (samples 30, sensible read) ✓
- [~] Intraday P&L dollars — pending first close (no positions yet)

**SMOKE TEST = PASS on the data layer.** Frontend visual render still needs an eyeball (`npm run dev`).

## OUT-OF-SAMPLE TRADE LEDGER (fixed size — this is the expectancy sample)
| # | time (CT) | setup (regime / GEX / RSI-50 / why) | side + strike | lots | credit | exit | $ P&L | book max-loss (% acct) | notes |
|---|-----------|-------------------------------------|---------------|------|--------|------|-------|------------------------|-------|
|   |           |                                     |               |      |        |      |       |                        |       |

**Running sample:** 0 / ~30–50 target  ·  cumulative $0  ·  win 0/0

## NEW-FEATURE OBSERVATIONS (watch only — do NOT trade off these yet)
- **Tail-Day Preview** — accurate vs your book? did it actually change a sizing decision?
  - 8:31 CT: live & correct with 0 positions ("No tail-day exposure"). Awaiting first position to test the $/% + green-days math.
- **ΔGEX velocity** — did it LEAD or LAG the regime/price turn? (decides whether we ever wire it into the core gate)
  - 8:31 CT (open): premarket −110M → −52M; gauge reads RISING +3.6M/min, **toward_flip TRUE, projected ~14 min to a positive flip** (flip 7552, spot 7529, −23 pts). **LEAD/LAG TEST OPEN:** does GEX actually cross positive in ~14 min? (Note: smoothed slope understated the −112→−52 spike — lags a sudden jump by a few polls.)
  - 8:37 CT: GEX −39M, gauge **+6.2M/min, flip ~6 min, spot_to_flip −15**; flip level drifted 7552→7557 with price. Approaching, NOT yet crossed. Regime turned **STATE A TREND-UP** (ER 0.82, trend_dom 90, RSI 69 overbought) off the gap-down. Still OPENING_DRIVE → stood aside.
  - **Note (flip-level question):** the "7552/7557" is the `zero_gamma_spx` Key Level ("Zero-Gamma Flip"); it re-prices every poll. Earlier-quoted 7552 was just a stale read of the same field.
- **Gamma flip (corrected)** — 8:31 CT: reads **7552, invariant-consistent** (spot 7529 < flip, net_gex −52M < 0 → "trend/vol side" ✓). Sensible level vs yesterday's ~6900 nonsense. Fix confirmed live.

## FINDINGS / NEXT-BUILD QUEUE (from live observation)
- **RSI basis mismatch (root-caused 8:12 CT):** engine RSI ≠ Robinhood RSI (today 66 vs 45). Cause: engine computes RSI-14 on **SPY via Alpaca IEX over a 500×5-min series with NO regular-hours filter** (`fetch_alpaca_market_data`, `bars?timeframe=5Min&limit=500`, default feed=IEX) → it **includes pre/after-market bars**, which *smear* the overnight gap across many small bars; Robinhood (RTH-only, consolidated, likely SPX) shows the gap as one big down-bar → much lower RSI on gap days. Method matches (both Wilder's). **CONFIRMED (8:25 CT): engine SPY RSI === Robinhood SPY RSI exactly** → feed (IEX vs consolidated) is NOT a factor; the whole 66-vs-45 gap is **SPY-vs-SPX** — SPY trades pre-market so its series smears the overnight gap; the SPX index is RTH-only so the gap is one jump. **Impact: the engine's RSI + everything derived (regime RSI score, bias, RSI-50 price, magnet) tracked SPY-with-overnight, not the SPX you trade — a hidden source of user/engine disagreement.** **SHIPPED LIVE (user's informed call, overriding freeze-the-core): RTH-only TA filter** — `config.TA_REGULAR_HOURS_ONLY=True` (kill-switch) + `data_fetcher._filter_to_rth` (drops pre/after-market bars; defensive <50-bar fallback) wired into `fetch_alpaca_market_data`. RTH-SPY ≈ SPX intraday, so RSI/EMA/CHOP/ER (and regime/bias/RSI-50/magnet) now track the SPX chart. +2 tests → **128**; synthetic_replay PASS. **Verify live: restart uvicorn → RSI should read ~SPX (mid-40s today).** Caveat: changes ALL TA basis mid-day → today's sample is split pre/post-config; note it for the expectancy log. Residual SPY-vs-SPX RTH tracking diff is tiny.
  - **8:31 CT update — bigger than expected:** live now SPX 49 / SPY 62 / engine 66. So SPY-vs-SPX RSI gap is ~13 pts (not tiny) — SPY (even on Robinhood) carries pre-market; SPX index is pure RTH. Engine still 66 ⇒ **RTH fix not yet live (uvicorn not restarted).** Guidance to user: **trade off Robinhood SPX RSI (49 = NEUTRAL, NOT the SPY 66 "overbought" read)**; engine RSI/RSI-50 are SPY-space — don't reconcile bar-for-bar. If post-restart RTH-SPY doesn't land near SPX, the real fix is **compute TA on SPX directly (^GSPC intraday) — do OFF-HOURS** (bigger change; today is for trading, not re-plumbing). RSI-50 has the same SPY-basis issue. **Lesson: I over-chased exactness mid-session and confused the user — simplify: one source of truth = the SPX chart.**
  - **8:45 CT — SHIPPED SPX-DIRECT RSI (the real fix).** Engine now computes RSI on the **SPX index (^GSPC, Yahoo)** directly, not the SPY proxy: `data_fetcher.fetch_spx_intraday_bars` (cached 60s) + `_align_rsi_to_index` (reindex ^GSPC RSI onto the SPY df's 5-min bars, nearest within 8 min); `main` overrides `df['RSI_14']` (flag `config.RSI_SOURCE_SPX=True`, full fallback to SPY RSI on any failure) BEFORE `analyze_market_regime`, so RSI + regime RSI-score + bias + story + magnet + RSI-50 (now a true SPX level, no ×ratio) all use SPX. EMA/CHOP/ER stay SPY-RTH (track SPX closely; VWAP needs SPY volume). +3 alignment tests → **132 total**; synthetic_replay PASS. **Yahoo blocked from sandbox → user verifies live: restart uvicorn, RSI should read ≈ Robinhood SPX (~49), not 66.** Caveat: depends on Yahoo ^GSPC intraday freshness (could lag a couple min); kill-switch `RSI_SOURCE_SPX=False`. **TODO (agent-doc sync owed): CLAUDE.md/.windsurfrules test count → 132 + RSI-basis pitfall (RTH filter + SPX-direct).**
  - **8:47 CT — fix NOT yet applied (engine still RSI 66 = SPY; SPX 51 / SPY 63 on Robinhood) → user hadn't restarted uvicorn.** Reminded: restart (positions persist in DB) → RSI should read ~51.
  - **NEW CRY-WOLF BUG (next-build):** user opened an iron condor (Call 7580 ×5 + Put 7525 ×5, ~$4,250/28% book — GOOD small sizing; tail-day banner correctly "above amber"). The Put 7525 got a **HIGH CLOSE rec ("Day low 7517 breached strike by 8.5 pts")** even though price had rallied **39 pts away** (moat 30.8, at_risk_side FALSE, +31%, HOLD_WITH_TRIGGER). The "day-low/high breached strike" CLOSE rec path in `generate_recommendations` was NOT covered by the #3 cry-wolf gate — **gate it on current `at_risk_side`/moat like the others.** (Card #2 reconciliation caught it → YELLOW "review", so the safety net held.) Live read given: trust the HOLD eval, ignore the rec; the CALL (trend-up day, at_risk) is the real leg to watch.
  - **RSI-basis consequence (validated live):** SPY RSI 66 "overbought → pullback" made the SHORT CALL look like a fade of an exhausted top; on SPX (RSI 51 neutral, ER 0.52 uptrend, gap rejected bullish) the call is actually fighting live momentum = genuinely the exposed leg. Confirms why SPX-basis matters for decisions.
  - **9:05 CT — SPX RSI STILL not landing after restart (engine 60 ≈ SPY 61, SPX 49).** uvicorn logs root-caused it: `SPX intraday bars: 330 ^GSPC 5m bars` (**fetch WORKS**) but `SPX RSI alignment insufficient — keeping SPY-based RSI`. Cause: Alpaca `limit=500` returns **only TODAY's bars (~81, pre-market-heavy; RTH filter saw only 15 RTH)**, so the nearest-within-8-min match overlapped just ~15 bars with the ^GSPC RTH grid → < 30 → bailed to SPY. **FIX: `_align_rsi_to_index` now FORWARD-FILLS** (each SPY bar takes the latest SPX RSI at-or-before it → the current bar always gets the latest ^GSPC RSI; robust to the today-only/sparse SPY index). +4 tests → **132**. RTH filter left as a documented no-op (can't get ≥50 RTH bars from today-only Alpaca; ThetaData has NO index subscription per logs, so Yahoo ^GSPC is the only SPX source). **User verifies: restart → RSI should now read ~49–51 (SPX), RSI-50 a true SPX level.** Confidence HIGH (fetch confirmed; ffill can't hit the <30 fallback). Caveat: still subject to Yahoo ^GSPC freshness (could be a few min stale, but SPX now, not SPY).
  - **9:15 CT — RESOLUTION (honest wall hit).** Post-restart the ffill fix WORKED (engine RSI became SPX-based, read **47**) — but that exposed the real problem: **Yahoo ^GSPC intraday LAGS**, so 47 was a STALE SPX RSI while live Robinhood SPX RSI was **59** at a new day high (a stale SPX RSI is WORSE for the engine's momentum/regime logic than a real-time SPY RSI). **Root constraint: no real-time SPX intraday feed exists for the engine** — Alpaca = SPY-only real-time; ThetaData has NO index subscription (per auth logs); Yahoo ^GSPC = SPX but laggy. So the engine CANNOT match a live SPX RSI. **DECISION: reverted `RSI_SOURCE_SPX=False`** (engine RSI back to real-time SPY) + reworded the `rsi_basis` caption to say plainly: *engine RSI is a real-time SPY momentum reference; read the SPX RSI off your SPX chart.* Code kept (flag-off) for a future **"stitch the fresh live ^GSPC quote onto the last bar before RSI"** improvement (off-hours, tested). **Meta-lesson: I over-engineered chasing exact RSI alignment across 3 live changes when the constraint (no real-time SPX feed) meant the answer was always "read RSI off the SPX chart; engine RSI = SPY momentum reference." Recognize data-feed constraints BEFORE building.**
  - **9:25 CT — trying the LIVE-QUOTE STITCH (user opted in).** Re-enabled `RSI_SOURCE_SPX=True`; `main` now overwrites the last ^GSPC bar's close with the FRESH live SPX quote (`fetch_spx_live_price`, cached) before computing RSI — same trick used for SPY's last candle — so the current RSI tracks real-time SPX even though Yahoo's intraday HISTORY lags. `.copy()` first (never mutate the cached frame); fully defensive (any failure → SPY RSI). Caption updated. 132 tests + replay green. **User verifies: restart → engine RSI should jump from ~47 toward ~live SPX (≈59); compare to the SPX chart.** Expected log line: `RSI source = SPX (^GSPC, live-stitched): last XX.X`. Risk: the missed move compresses into one bar so RSI may slightly overshoot; if Yahoo history is very stale it's imperfect — but far closer than the stale 47. If it overshoots/misbehaves → kill-switch `RSI_SOURCE_SPX=False`.
  - **9:35 CT — STITCH INSUFFICIENT → RSI CHASE CLOSED (final).** Live-stitched engine RSI = **53** vs Robinhood SPX **67** (SPY 74) on the rally to new highs — still ~14 off. Root reason: Yahoo ^GSPC intraday history is stale AND **RSI-14 is a 14-bar smoothed average**, so stitching ONE fresh bar can't rebuild the missing recent rally bars. **No real-time SPX feed exists for the engine** (Alpaca=SPY, ThetaData=no index sub, Yahoo=laggy) → it cannot match real-time SPX RSI. **DECISION (final): `RSI_SOURCE_SPX=False`, engine RSI = real-time SPY momentum reference; user reads SPX RSI off the SPX chart.** Caption reworded to say so. Code left in place (off). **4 attempts (RTH filter → nearest → ffill → live-stitch) — overran; should have committed to "no real-time SPX feed → use the chart" far sooner.**
  - **⚠️ 9:35 CT — BOOK RISK FLAG (more urgent than the RSI):** while focused on RSI, the book grew to **3 call spreads (7580 ×5 + 7590 ×20) vs 1 put (7525 ×5) = 88% of account, OVER CAP**, into a rally to new day highs (ER 0.64, trend strengthening). 7580 call in **GAMMA TRAP / CLOSE_NOW**; tail-day preview RED (over_cap); book **−$750**. The oversized-short-into-a-trend pattern recurred (call side). Positive GEX + 7585 call wall *may* cap it (FADE ON), but 88% sizing makes it a bad bet. Flagged to user: cut the 7580 per the engine; trim the call-heavy book. **Meta: the RSI rabbit hole distracted from real risk management — the engine's risk signals (gamma trap, over-cap tail-day, CLOSE_NOW) were working the whole time.**

## FIRST DATA-ERA TAIL DAY (the key missing observation — fill ONLY if it happens)
- A losing day, especially a gap / ITM-settlement: what happened, did fixed size keep it survivable,
  did the force-path fire and did you obey it? (One such day teaches more than ten green ones.)

## EOD (light)
- Trades today / cumulative sample size:
- Realized P&L (contract-correct):
- Did size discipline hold? (did the Tail-Day banner ever go red?):
- New features — any bug, any genuinely useful read?
- One line for tomorrow (NFP Friday):
